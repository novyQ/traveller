export type VisitedInputType = {
  variables: {
    input: {
      id: number
      visited: boolean
    }
  }
}

export type WishlistInputType = {
  variables: {
    input: {
      id: number
      wishlist: boolean
    }
  }
}
